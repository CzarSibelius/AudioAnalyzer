namespace AudioAnalyzer.Visualizers;

/// <summary>Optional rotation applied to the mirrored (destination) region.</summary>
public enum MirrorRotation
{
    /// <summary>No rotation; mirrored content is shown as-is.</summary>
    None,

    /// <summary>Flip the mirrored region 180° (both axes).</summary>
    Flip180
}
