namespace AudioAnalyzer.Application.Abstractions;

/// <summary>Summary entry for a charset file (id = filename without extension).</summary>
public sealed record CharsetInfo(string Id, string Name);
