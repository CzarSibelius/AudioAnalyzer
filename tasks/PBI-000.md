# PBI-000: Template (delta)

**Numbering:** `tasks/PBI-000.md` is this **template** only. Numbered backlog deltas use **`tasks/PBI-001-<slug>.md`**, **`PBI-002-<slug>.md`**, … (three-digit sequence + kebab-case slug). Pick the **next unused** integer after the highest existing `PBI-NNN-*.md` in `tasks/`.

**Transient work item** — close after merge. **The Spec** (`specs/<domain>/spec.md`) holds **state**; this file holds **delta**.

## Directive

What to change, with explicit file and scope boundaries (what is in scope / out of scope).

## Context pointer

- Primary spec: `specs/<domain>/spec.md`
- Related ADRs: `docs/adr/...`

## Verification pointer

- Contract sections to satisfy: **Definition of Done**, **Regression guardrails**, **Scenarios** in the spec above.
- Build / test commands: see [AGENTS.md](../AGENTS.md).

## Refinement rule

If implementation discovers missing constraints or wrong behavior in the spec: **update the spec in the same commit** (same-commit rule). If the change is product-level or ambiguous, stop and flag for human review.
